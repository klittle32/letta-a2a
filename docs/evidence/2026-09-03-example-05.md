# Example 05 evidence — 2026-09-03

This is the concise execution record for [external A2A agent to Letta](../../examples/05-external-a2a-agent-to-letta/).

## Documented command

The ordinary Compose lab was started and the README's exact smoke command was run:

```bash
node scripts/smoke-a2a.mjs reference-agent \
  "ask-letta Reply with exactly EXTERNAL_TO_LETTA_OK and nothing else."
```

Observed outer task result:

```json
{
  "status": { "state": "TASK_STATE_COMPLETED" },
  "artifacts": [
    {
      "name": "response",
      "parts": [{ "text": "EXTERNAL_TO_LETTA_OK" }]
    }
  ]
}
```

The ordinary lab was then stopped with `docker compose down`; its named volumes were preserved.

## Automated evidence

The final provider-backed matrix passed through one shared agentgateway process:

```text
✓ Letta Agent Card preservation and URL rewriting
✓ provider-backed Letta Agent A to independent reference agent
✓ independent reference agent to Letta Agent A delegation
✓ outer cancellation propagates to the remote child task

Independent A2A integration: PASS
```

The reverse-direction implementation also passed:

```text
20 Python tests
25 Bun tests
Python Ruff lint and formatting
TypeScript no-emit check and production build
Compose validation
provider-free protocol matrix
local Markdown links and diff hygiene
```

The Python tests cover official SDK discovery/send/poll behavior, exact-card identity validation, hop metadata, one overall deadline, completed and failed child mapping, and stable local cancellation. Cancellation of the accepted remote Letta child is explicitly not claimed by this example; an already accepted child may continue after the outer call is canceled or times out.
