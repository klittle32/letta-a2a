# Example 06 evidence — 2026-09-03

This is the concise execution record for [static Bearer authentication](../../examples/06-static-bearer-auth/).

## Documented requests

The ordinary Compose lab was started with the README's disposable key. The three documented Agent Card requests returned:

```text
missing=401
wrong=401
valid=200
```

The valid response contained:

```json
{
  "securitySchemes": {
    "a2aLabBearer": {
      "httpAuthSecurityScheme": {
        "description": "Static lab-only Bearer key enforced by agentgateway.",
        "scheme": "Bearer",
        "bearerFormat": "opaque"
      }
    }
  },
  "securityRequirements": [
    { "schemes": { "a2aLabBearer": {} } }
  ]
}
```

The documented service-log check passed. The ordinary lab was stopped with `docker compose down`; named volumes were preserved.

## Automated evidence

Both managed matrices passed. The provider-free run included:

```text
✓ missing, incorrect, and valid gateway authentication
✓ Agent Card discovery through agentgateway
✓ service logs omit tested credentials

Independent A2A integration: PASS
```

The provider-backed run additionally verified Agent A's proxied card and the complete bidirectional delegation/cancellation path before repeating the service-log leak check.

The integration authentication probe covers both Agent Card GET and JSON-RPC POST: missing and incorrect credentials return `401`, while the valid RPC request reaches the backend and returns its expected task-not-found A2A envelope with HTTP `200`.

Both implementations emit the canonical A2A 1.0 security shape. The bridge uses the JavaScript SDK's protobuf JSON serializer, and the Python fixture uses `MessageToDict` directly rather than the SDK route helper that injects legacy compatibility fields.

A final fetch of both gateway-published cards confirmed that each exposed only the canonical top-level `securitySchemes` and `securityRequirements` fields, with no flattened legacy security fields.

One provider-backed attempt failed only because the model wrapped the requested `LETTA_REFERENCE_OK` marker in explanatory text. The identical retry passed; static authentication assertions passed in both attempts.

Final local gates:

```text
25 Bun tests
20 Python tests
Python Ruff lint and formatting
TypeScript no-emit check and production build
Compose validation
local Markdown links and diff hygiene
```

The integration leak check compares raw service logs in memory against the valid and incorrect gateway keys, provider credential, and App Server token. On any other integration failure, captured logs are redacted before they are printed.
